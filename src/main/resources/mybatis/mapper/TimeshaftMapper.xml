<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.timeshaft.after_end.mapper.TimeshaftMapper">

    <resultMap id="BaseResultMap" type="Timeshaft">
        <!--@Table timeshaft-->
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="groupId" column="group_id" jdbcType="INTEGER"/>
        <result property="creatorId" column="creator_id" jdbcType="INTEGER"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="beginTime" column="begin_time" jdbcType="TIMESTAMP"/>
        <result property="endTime" column="end_time" jdbcType="TIMESTAMP"/>
        <result property="conclude" column="conclude" jdbcType="VARCHAR"/>
        <result property="type" column="type" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="BaseResultMap">
        select
          id, group_id, creator_id, name, begin_time, end_time, conclude, type
        from timeshaft.timeshaft
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="BaseResultMap">
        select
          id, group_id, creator_id, name, begin_time, end_time, conclude, type
        from timeshaft.timeshaft
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="BaseResultMap">
        select
          id, group_id, creator_id, name, begin_time, end_time, conclude, type
        from timeshaft.timeshaft
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="groupId != null">
                and group_id = #{groupId}
            </if>
            <if test="creatorId != null">
                and creator_id = #{creatorId}
            </if>
            <if test="name != null and name != ''">
                and name = #{name}
            </if>
            <if test="beginTime != null">
                and begin_time = #{beginTime}
            </if>
            <if test="endTime != null">
                and end_time = #{endTime}
            </if>
            <if test="conclude != null and conclude != ''">
                and conclude = #{conclude}
            </if>
            <if test="type != null and type != ''">
                and type = #{type}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into timeshaft.timeshaft(group_id, creator_id, name, begin_time, end_time, conclude, type)
        values (#{groupId}, #{creatorId}, #{name}, #{beginTime}, #{endTime}, #{conclude}, #{type})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update timeshaft.timeshaft
        <set>
            <if test="groupId != null">
                group_id = #{groupId},
            </if>
            <if test="creatorId != null">
                creator_id = #{creatorId},
            </if>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="beginTime != null">
                begin_time = #{beginTime},
            </if>
            <if test="endTime != null">
                end_time = #{endTime},
            </if>
            <if test="conclude != null and conclude != ''">
                conclude = #{conclude},
            </if>
            <if test="type != null and type != ''">
                type = #{type},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from timeshaft.timeshaft where id = #{id}
    </delete>

    <select id="queryTimeshaftByTime" resultMap="BaseResultMap">
        select
            id, group_id, creator_id, name, begin_time, end_time, conclude, type
        from timeshaft.timeshaft
        where (begin_time between #{start_time} and #{end_time}) and (end_time between #{start_time} and #{end_time})
    </select>

</mapper>