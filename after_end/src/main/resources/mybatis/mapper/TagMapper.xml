<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.timeshaft.after_end.mapper.TagMapper">

    <resultMap id="BaseResultMap" type="Tag">
        <!--@Table tag-->
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="timeshaftId" column="timeshaft_id" jdbcType="INTEGER"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="BaseResultMap">
        select
          id, timeshaft_id, name
        from timeshaft.tag
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="BaseResultMap">
        select
          id, timeshaft_id, name
        from timeshaft.tag
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="BaseResultMap">
        select
          id, timeshaft_id, name
        from timeshaft.tag
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="timeshaftId != null">
                and timeshaft_id = #{timeshaftId}
            </if>
            <if test="name != null and name != ''">
                and name = #{name}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into timeshaft.tag(timeshaft_id, name)
        values (#{timeshaftId}, #{name})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update timeshaft.tag
        <set>
            <if test="timeshaftId != null">
                timeshaft_id = #{timeshaftId},
            </if>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from timeshaft.tag where id = #{id}
    </delete>

</mapper>